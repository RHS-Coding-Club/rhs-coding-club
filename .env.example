# This project does not read a .env file.
#
# Local secrets live in .dev.vars (copy .dev.vars.example), which Wrangler and
# the Vite dev server both load. Public config lives in wrangler.jsonc "vars".
# Production secrets are set with `wrangler secret put NAME`.
